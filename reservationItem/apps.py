from django.apps import AppConfig


class ReservationitemConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'reservationItem'
    verbose_name = "Gestión de reservas"
